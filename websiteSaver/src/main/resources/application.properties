# ------------------ IO related ---------------------------------------
# HtmlUtil - 10s connection timeout
html.connect.timeout=10000
# HtmlUtil - 60s read timeout
html.read.timeout=60000
# server's port
server.port=8443

# ------------------ function related ----------------------------------
# root directory for pdf downloaded (for convenience)
rootDir=/home/zhuangyongj/viw/pdfs

# ------------------ data source ---------------------------------------
spring.datasource.url=jdbc:mysql://localhost:3306/websiteSaver
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ------------------ SSL Certificate -----------------------------------
# The format used for the keystore. It could be set to JKS in case it is a JKS file
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:cert/websiteSaver-yj.p12
# The password used to generate the certificate
server.ssl.key-store-password=zhuangyongj
# The alias mapped to the certificate
server.ssl.key-alias=websiteSaver
server.ssl.enabled=true

# ------------------- for debug only -----------------------------------
#spring.security.user.name=user
#spring.security.user.password=password
